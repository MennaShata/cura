/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DrugPipe } from './drug.pipe';

describe('Pipe: Druge', () => {
  it('create an instance', () => {
    let pipe = new DrugPipe();
    expect(pipe).toBeTruthy();
  });
});
