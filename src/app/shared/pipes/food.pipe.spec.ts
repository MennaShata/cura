/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FoodPipe } from './food.pipe';

describe('Pipe: Foode', () => {
  it('create an instance', () => {
    let pipe = new FoodPipe();
    expect(pipe).toBeTruthy();
  });
});
